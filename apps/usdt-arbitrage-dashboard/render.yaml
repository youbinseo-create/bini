services:
  - type: web
    name: usdt-arbitrage-dashboard
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: "node server.mjs"
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
