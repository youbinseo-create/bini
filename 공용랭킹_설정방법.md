# 카드 놀이터 공용 랭킹 설정 방법

현재 게임은 GitHub Pages에서 무료로 열립니다. 공용 랭킹은 Google Sheets와 Apps Script를 사용합니다.

## 1. Google Sheet 만들기

1. Google Drive에서 새 스프레드시트를 만듭니다.
2. 이름은 `카드 놀이터 랭킹`처럼 알아보기 쉽게 정합니다.

## 2. Apps Script 붙이기

1. 스프레드시트 메뉴에서 `확장 프로그램` -> `Apps Script`를 엽니다.
2. 기본 코드 내용을 모두 지웁니다.
3. 이 저장소의 `google-apps-script-ranking.js` 내용을 그대로 붙여 넣습니다.
4. 저장합니다.

## 3. 웹앱으로 배포하기

1. Apps Script 오른쪽 위 `배포` -> `새 배포`를 누릅니다.
2. 유형에서 `웹 앱`을 고릅니다.
3. 실행 권한은 `나`로 둡니다.
4. 액세스 권한은 `모든 사용자`로 둡니다.
5. 배포 후 나오는 웹앱 URL을 복사합니다.

## 4. 게임에 URL 넣기

`index.html`에서 아래 줄을 찾습니다.

```js
const RANKING_API_URL = "";
```

따옴표 안에 Apps Script 웹앱 URL을 넣습니다.

```js
const RANKING_API_URL = "https://script.google.com/macros/s/여기에_긴_ID/exec";
```

그 다음 GitHub에 다시 올리면 모든 폰에서 같은 공용 랭킹을 봅니다.

## 주의

이 방식은 가족/수업용으로 가볍게 쓰기 좋은 무료 구조입니다. 웹앱 주소를 아는 사람은 기록을 보낼 수 있으므로, 아주 중요한 서비스나 공개 대회용으로는 부족합니다.
